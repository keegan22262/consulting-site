/**
 * SectionDivider — Now a zero-height spacer (sections flow edge-to-edge).
 * Kept as a component to avoid import errors; renders nothing.
 */
export default function SectionDivider() {
  return null;
}
